{% extends ":Back:base.html.twig" %}
{% block title %}Administrator{% endblock %}
{% block body %}
    <div class="container-fluid container-fullw bg-white">
        <div class="row">
            <div class="col-md-12">
                <h5 class="over-title margin-bottom-15"><span class="text-bold">Formulaire</span></h5>
                <form action="{{ path('administrator',{'id':user.id}) }}" method="post" role="form" id="form">
                    {{ form_widget(form._token) }}
                    <div class="row">
                        {{ formulaire.input("col-md-3","User name",form.username,true) }}
                        {{ formulaire.input("col-md-3","Email",form.email,true) }}
                    </div>
                    <div class="row">
                        {{ formulaire.input("col-md-3","Roles",form.roles,true) }}
                    </div>
                    <div class="row">
                        {{ formulaire.input("col-md-3","Mot de passe",form.plainPassword.first,true) }}
                    </div>
                    <div class="row">
                        {{ formulaire.input("col-md-3","Mot de passe confirmation",form.plainPassword.second,true) }}
                    </div>
                    {{ macro.enregistrer }}
                </form>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <h5 class="over-title margin-bottom-15"><span class="text-bold">List</span></h5>
                <table class="table table-striped table-bordered table-hover table-full-width" id="sample_1">
                    <thead>
                        <tr>
                            <th>Username</th>
                            <th>Roles</th>
                            <th>Email</th>
                            <th>State</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for user in users %}
                            <tr>
                                <td>{{ user.username }}</td>
                                <td>
                                    {% for role in user.roles if role!= 'ROLE_USER' %}
                                        - {{ role }} <br>
                                    {% endfor %}
                                </td>
                                <td>{{ user.email }}</td>
                                <td>
                                    {% if user.enabled %}
                                        <a href="{{ path('enabled_user',{'id':user.id}) }}" ><span class="label label-sm label-success">Enabled</span></a>
                                    {% else %}
                                        <a href="{{ path('enabled_user',{'id':user.id}) }}"><span class="label label-sm label-danger">Disabled</span></a>
                                    {% endif %}
                                </td>
                                <td>
                                    {{ macro.edit(path('administrator',{'id':user.id})) }}
                                    {% if user != currentUser %}
                                        {{ macro.delete(path('delete_administrator',{'id':user.id})) }}
                                    {% endif %}
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

    </div>

{% endblock %}