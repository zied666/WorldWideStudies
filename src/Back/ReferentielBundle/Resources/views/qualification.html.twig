{% extends ":Back:base.html.twig" %}
{% block title %}Qualifications{% endblock %}
{% block body %}
    <div class="container-fluid container-fullw bg-white">
        <div class="row">
            <div class="col-md-12">
                <form action="{{ path('qualification',{'id':qualification.id}) }}" {{ form_enctype(form) }}  method="post" role="form" id="form">
                    {{ form_widget(form._token) }}
                    <div class="row">
                        {{ formulaire.input("col-md-3","Name",form.name,true) }}
                        {{ formulaire.select2("col-md-3","Level of study",form.level,true) }}
                    </div>
                    {{ macro.enregistrer }}
                </form>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <h5 class="over-title margin-bottom-15"><span class="text-bold">List</span></h5>
                <table class="table table-striped table-bordered table-hover table-full-width" id="sample_1">
                    <thead>
                        <tr>
                            <th class="center">Name</th>
                            <th class="center">Level of study</th>
                            <th class="center">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for qualification in qualifications %}
                            <tr>
                                <td class="center">{{ qualification.name }}</td>
                                <td class="center">{{ qualification.level }}</td>
                                <td class="center">
                                    {{ macro.edit(path('qualification',{'id':qualification.id})) }}
                                    {{ macro.delete(path('delete_qualification',{'id':qualification.id})) }}
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

    </div>

{% endblock %}